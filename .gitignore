# Node dependencies
node_modules/

# Lock file (keep this in repo, so don’t ignore package-lock.json)
# Agar ignore karna ho to uncomment karo ↓
# package-lock.json

# Env & sensitive keys
.env
.env.local
.env.*.local
serviceAccountKey.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Build / dist output
dist/
build/

# MacOS system files
.DS_Store

# Local Vercel files (don’t push local config)
.vercel/

# Optional: editor/IDE settings
.vscode/
.idea/
